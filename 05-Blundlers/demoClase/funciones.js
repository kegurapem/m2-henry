// const {num1, num2, nombre} = require('./datos')
// console.log(datos);

const sumar = (a, b) => console.log(a + b)

const printName = (name) => {
    console.log(name)
}

module.exports = {
    sumar,
    printName
}